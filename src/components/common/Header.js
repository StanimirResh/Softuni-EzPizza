import { TopBarNav } from "../navigation/TopBarNav"
import { Navigation } from "../navigation/Navigation"

export const Header = () => {
    return (
        <header>
            <TopBarNav />
            <Navigation />
        </header>
    )
}