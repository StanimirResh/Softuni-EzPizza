import { TopBarNav } from "../Navigate/TopBarNav"
import { Navigation } from "../Navigate/Navigation"

export const Header = () => {
    return (
        <header>
            <TopBarNav />
            <Navigation />
        </header>
    )
}